spring:
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:test;MODE=PostgreSQL
    jpa:
      hibernate:
        ddl-auto: create
        
---

spring:
  profiles: staging
  datasource:
    url: ${JDBC_DATABASE_URL}
    jpa:
      hibernate:
        ddl-auto: update
        
